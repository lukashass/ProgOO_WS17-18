package programming.set12.exceptions;

public class ExceptionalExceptionHandlerThatAlsoCalculatesStuff {
	void calculate(InputOutputProvider provider) {
		// String lineOne = provider.readLine();
		// if(lineOne.equals("+") || )
	}
}
